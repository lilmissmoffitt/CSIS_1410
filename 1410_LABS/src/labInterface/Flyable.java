package labInterface;

public interface Flyable {
	/**
	 * method launch prints a statement that describes how the flyable object
	 * launches
	 */
	void launch();

	/**
	 * method launch prints a statement that describes how the flyable object
	 * launches
	 */
	void land();
}
